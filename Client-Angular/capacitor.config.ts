import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ibtikar.feeCalculator',
  appName: 'calc-fees',
  webDir: 'dist/calc-fees/browser'
};

export default config;
